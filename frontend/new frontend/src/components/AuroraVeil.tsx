import React from 'react';

/* AuroraVeil
   Renders two layered background effects (Aurora + Dark Veil noise/vignette) for all
   content below the Hero. Positioned relative to document flow (not covering Hero),
   but uses fixed internal layers for continuous motion while scrolling.

   Accessibility: respects reduced-motion preference (animation disabled).
*/
// Component now returns null since aurora / veil are disabled.
const AuroraVeil: React.FC = () => null;

export default AuroraVeil;
